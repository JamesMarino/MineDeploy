const styles = theme => ({
    container: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-around',
        minHeight: '100vh',
        backgroundColor: theme.primaryBackgroundColor,
    },
});

export default styles;
